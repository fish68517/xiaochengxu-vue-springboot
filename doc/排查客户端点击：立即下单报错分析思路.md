你现在是本项目的代码审计工程师。

项目根目录：
E:\bishe27\服务平台小程序\code

背景：
当前 client 已经成功编译并导入微信开发者工具。
微信小程序首页已经可以正常显示商品，说明：
- 微信小程序可以正常启动
- 当前品牌 demo-a 已经可以正常解析
- listProducts 已经可以正常返回商品

在微信开发者工具 Console 中执行：

wx.getAccountInfoSync().miniProgram.appId

返回真实微信小程序 AppID：

wxe40bb897376601cc

执行：

wx.login({
  success: res => console.log(res),
  fail: err => console.error(err)
})

返回：

errMsg = "login:ok"
并成功拿到 code

但是：

点击商品“立即下单”后，
Network 中实际看到 uniCloud 请求：

action = "h5Token"

请求大致如下：

functionTarget = game-service
functionArgs.action = h5Token
payload.productId = 某商品ID

同时请求里带有 CUSTOMER token，
token 解码后可以看到：

roles = ["CUSTOMER"]
brandScopes = ["demo-a"]

但是后端返回：

{
  "ok": false,
  "code": "UNAUTHORIZED",
  "message": "会话已失效，请重新登录"
}

当前疑问：

1. 为什么微信小程序环境 mp-weixin 点击“立即下单”会调用 h5Token？
2. h5Token 是否本来只应该用于 H5？
3. 当前微信小程序正确登录链是否应该是：

wx.login
→ miniLogin
→ jscode2session
→ openid
→ CUSTOMER Session
→ createOrder
→ 微信支付

4. 当前实际实现中：
- miniLogin 在什么情况下调用？
- ensureSession 如何判断是否需要 miniLogin？
- h5Token 在什么场景调用？
- “立即下单”页面到底调用了什么函数？
- 微信小程序和 H5 是否共用了同一套下单跳转逻辑？
- CUSTOMER token 为什么存在，但 h5Token 仍返回 UNAUTHORIZED？

请严格只读分析，不要修改任何代码。

==================================================
一、必须追踪的前端调用链
==================================================

请从 client 中搜索并完整追踪：

- 点击“立即下单”的页面
- 商品详情页
- ensureSession
- miniLogin
- wx.login
- h5Token
- createOrder
- wx.requestPayment
- requestPayment
- openH5
- navigateTo
- redirectTo
- location.href
- window.location
- token/session 保存逻辑

重点目录：

apps/client/src

请给出真实调用链，例如：

pages/product/detail.vue
→ onBuy()
→ ensureSession()
→ xxx()
→ api.h5Token()
→ ...

或者实际代码中的真实函数名。

每一步必须写：
- 文件路径
- 行号
- 函数名
- 调用下一个函数的位置
- PLATFORM 分支条件

==================================================
二、重点确认 mp-weixin 和 H5 是否分支正确
==================================================

请检查所有：

#ifdef H5
#ifdef MP-WEIXIN
#ifndef
uni.getSystemInfo
process.env
uniPlatform
PLATFORM
mp-weixin
H5

相关代码。

重点回答：

当前“立即下单”逻辑是否存在类似：

H5：
h5Token
→ 跳转 H5 下单页

MP-WEIXIN：
也错误复用了 h5Token
→ 导致微信小程序没有走原生下单

如果不是，请说明真实设计。

==================================================
三、追踪 miniLogin 完整流程
==================================================

请找到：

miniLogin 前端调用入口
→ wx.login
→ code
→ game-service action = miniLogin
→ 后端 miniLogin service
→ oauth / jscode2session
→ openid
→ CUSTOMER user
→ session/token 生成
→ 前端保存 token

请明确：

1. miniLogin 是否在 App.vue 启动时自动调用？
2. 是否只在 ensureSession 时调用？
3. 哪些条件下会跳过 miniLogin？
4. 如果本地已经存在 CUSTOMER token，是否会直接复用？
5. token 是否只做 JWT 本地校验，还是还会查 auth_sessions？
6. token 有效但服务端 session 记录不存在时，是否会返回 UNAUTHORIZED？

==================================================
四、追踪 h5Token 完整流程
==================================================

请找到：

前端：
api.h5Token
调用位置
调用条件
返回值如何使用

后端：
game-service
action = h5Token
对应 service
auth 要求
session 校验
productId 校验
brand 校验
token 生成逻辑

请重点回答：

h5Token 的设计目的究竟是什么？

是：

A. 仅供 H5 下单页面使用

还是：

B. 微信小程序先通过 CUSTOMER Session 获取 h5Token，
   再跳转 H5 页面完成订单/支付

还是：

C. 其他设计

不要根据函数名猜，必须根据真实源码说明。

==================================================
五、分析为什么已有 CUSTOMER token 仍然 UNAUTHORIZED
==================================================

当前请求中已经带 token，payload 解码可见：

userId = customer-...
roles = CUSTOMER
brandScopes = demo-a

但是服务端返回：

UNAUTHORIZED
会话已失效，请重新登录

请查清楚 auth.cjs / session 校验逻辑。

重点确认：

1. JWT 是否过期？
2. auth_sessions 是否必须存在？
3. token 中的 userId 是否必须在 users 中存在？
4. securityVersion 是否需要匹配？
5. session 是否可能在 clearStorage / bootstrap / 重新部署后失效？
6. token 是否来自本地旧环境/旧登录？
7. h5Token 是否要求比普通 CUSTOMER token 更严格的 session？
8. 是否出现：
   token 结构合法
   但数据库 session 已不存在
   → UNAUTHORIZED
这种情况？

==================================================
六、检查当前微信小程序支付设计
==================================================

只做代码审计，不要改代码。

请搜索：

wx.requestPayment
requestPayment
createPayment
preparePayment
payment
prepay
wechat
mchid
WECHAT_PAY
wechat-callback

说明当前微信小程序支付设计到底是哪一种：

方案 1：
微信小程序
→ createOrder
→ 后端创建微信支付 prepay
→ wx.requestPayment

方案 2：
微信小程序
→ h5Token
→ 跳 H5 页面
→ H5 支付

方案 3：
混合模式

必须以当前代码为准。

==================================================
七、输出结论
==================================================

请最终输出：

docs/debug/MP_WEIXIN_ORDER_LOGIN_AUDIT.md

文档必须包含：

# 1. 当前真实调用链

例如：

点击立即下单
→ xxx
→ xxx
→ h5Token
→ ...

# 2. miniLogin 实际调用链

# 3. h5Token 实际用途

# 4. mp-weixin 与 H5 差异

# 5. UNAUTHORIZED 根因候选

每个候选标记：

CONFIRMED
HIGH_PROBABILITY
POSSIBLE
NOT_SUPPORTED_BY_CODE

# 6. 当前微信小程序正确业务链

根据代码实际情况给出：

AppID
→ Brand
→ wx.login
→ miniLogin
→ CUSTOMER Session
→ Product
→ Order
→ Payment

或者如果项目实际不是这样，也必须给出真实设计。

# 7. 当前问题最小修复建议

注意：
只给建议，不改代码。

优先给出最小改动方案，不要大重构。

==================================================
八、非常重要的约束
==================================================

1. 不允许直接修改代码。
2. 不允许根据函数名猜用途。
3. 所有结论必须有：
   文件路径 + 行号 + 函数名。
4. 必须区分：
   - H5
   - mp-weixin
   - uniCloud
   - 微信原生 API
5. 不允许把 DCloud AppID 和微信 AppID 混淆。
6. 当前 DCloud AppID：
   __UNI__80260AB
7. 当前微信 AppID：
   wxe40bb897376601cc
8. 当前品牌：
   brandId = demo-a
9. 不要修改支付配置。
10. 不要部署。
11. 不要上传微信平台。
12. 不要修改云函数环境变量。
13. 先完成代码审计，再停止。

==================================================
九、最后额外输出一个极简结论
==================================================

最后在终端只打印：

微信小程序当前下单链：
xxxxx

h5Token 是否合理：
是 / 否 / 条件合理

miniLogin 是否被正确调用：
是 / 否 / 条件触发

UNAUTHORIZED 最可能原因：
xxxxx

建议下一步：
xxxxx
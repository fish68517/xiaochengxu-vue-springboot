请帮我修复微信小程序 WebView 中 H5 下单页提示“未配置服务号网页授权”的问题。

项目根目录：
E:\bishe27\服务平台小程序\code

已确认：
1. game-service 云函数环境变量已经配置：
   OAUTH_APPID
   OAUTH_SECRET

2. apps/client/src/api.js 当前代码为：

export const OAUTH_APPID = import.meta.env.VITE_OAUTH_APPID || '';

3. H5 下单页：
apps/client/src/pages/h5-order/index.vue

会在微信小程序 WebView 中调用 redirectToOauth()，
当前因为前端 VITE_OAUTH_APPID 为空，提示：
“未配置服务号网页授权”。

请直接修改代码和配置，要求：

一、前端配置
检查：

apps/client/.env.production

确保存在：

VITE_OAUTH_APPID=<服务号AppID>

注意：
- 只允许把服务号 AppID 放到前端
- 禁止把 OAUTH_SECRET / AppSecret 写入前端、Vue、api.js、.env.production 的 VITE_* 变量或 Git
- OAUTH_SECRET 继续只使用 game-service 云函数环境变量

二、代码检查
确认：

apps/client/src/api.js

仍然通过：

import.meta.env.VITE_OAUTH_APPID

读取前端 AppID。

确认：

apps/client/src/pages/h5-order/index.vue

中的 redirectToOauth()

使用该 OAUTH_APPID 拼接微信网页 OAuth URL。

如果当前代码已经正确，不要大改业务流程。

三、增加最小调试日志
在 redirectToOauth() 中增加：

console.log('[OAuth DEBUG] OAUTH_APPID =', OAUTH_APPID);
console.log('[OAuth DEBUG] current href =', location.href);

禁止打印：
- OAUTH_SECRET
- AppSecret
- 完整敏感 token

四、检查生产构建
完成修改后，检查：

apps/client/.env.production
apps/client/src/api.js
apps/client/src/pages/h5-order/index.vue

并说明必须重新执行 HBuilderX Web/H5 发行，
生成：

apps/client/dist/build/web

因为微信小程序 WebView 加载的是：

https://h5.qmhyjoy.com

不是 mp-weixin 本地代码。

五、不要做这些操作
- 不要修改 game-service 的 OAUTH_APPID / OAUTH_SECRET
- 不要重新生成 Secret
- 不要修改微信支付配置
- 不要修改品牌逻辑
- 不要修改 h5Token 逻辑
- 不要部署云函数
- 不要上传微信小程序

六、完成后终端只输出：

VITE_OAUTH_APPID 是否已配置：
是 / 否

前端是否包含 OAUTH_SECRET：
是 / 否（必须为否）

redirectToOauth 是否使用 VITE_OAUTH_APPID：
是 / 否

是否需要重新构建 Web/H5：
是

修改文件：
xxx
xxx
# 腾讯云单机双项目隔离部署方案
## 萌宠商城 + 电梯维修：独立灰度、可快速迁移、可快速删除

> 版本：V1.0  
> 适用环境：腾讯云 Windows Server / 轻量应用服务器  
> 适用项目架构：FastAPI + MySQL 8 + Nginx + Web 管理后台 + uni-app 小程序/H5  
> 设计目标：在**不影响现有萌宠商城灰度测试**的前提下，把“电梯维修”临时部署到同一台腾讯云服务器；未来电梯维修获得独立服务器后，可快速迁走并在旧服务器上完整清理。

---

# 1. 需求结论：合理，而且适合当前阶段

当前需求是合理的。

两个项目架构相似时，共用一台 Windows 云服务器做早期灰度，可以明显减少：

- 新服务器采购等待时间；
- 重复安装 Python / MySQL / Nginx 的时间；
- 域名、备案、SSL 尚未完成时的阻塞；
- 前后端联调成本。

但是必须遵守一个原则：

> **共享“服务器资源”，不共享“项目运行目录、虚拟环境、数据库 Schema、数据库账号、FastAPI 端口、Nginx 配置目录、日志目录、计划任务和灰度端口”。**

这样未来迁移电梯维修项目时，只需要：

```text
导出 elevator_repair_prod
→ 复制 C:\ElevatorRepair
→ 新服务器恢复
→ 验证
→ 删除旧服务器 ElevatorRepair 相关任务/端口/数据库/目录
```

萌宠商城无需停止，也不需要重新部署。

---

# 2. 推荐的隔离级别

## 2.1 推荐方案

采用：

```text
同一台 Windows Server
同一个 MySQL80 服务
两个完全独立数据库
两个独立数据库账号
两个独立 Python venv
两个独立 FastAPI 端口
两个独立 Nginx 实例
两个独立计划任务
两个独立灰度端口组
两个独立项目根目录
```

这是“快速部署”和“可迁移隔离”之间最合适的平衡。

## 2.2 为什么不建议再安装第二套 MySQL

理论上可以：

```text
萌宠 MySQL → 3306
电梯 MySQL → 3307
```

但这会增加：

- 第二套 MySQL Data 目录；
- 第二个 Windows Service；
- 第二套内存缓存；
- 第二套备份维护；
- 第二套升级和故障排查。

对于早期灰度测试没有必要。

推荐：

```text
MySQL80 :3306
│
├─ pet_life_prod
│   └─ 萌宠商城
│
└─ elevator_repair_prod
    └─ 电梯维修
```

数据库通过 **Schema + 独立用户授权** 隔离。

未来迁移：

```text
mysqldump elevator_repair_prod
```

即可把电梯项目数据库完整带走。

---

# 3. 最终端口规划

## 3.1 已有萌宠商城

保持现状，不修改：

| 端口 | 服务 | 公网 |
|---|---|---:|
| `80` | 萌宠 Web 后台 / API IP 灰度 | ✅ |
| `8081` | 萌宠 H5 灰度 | ✅ |
| `8000` | 萌宠 FastAPI | ❌，仅 127.0.0.1 |
| `3306` | MySQL80 | ❌，仅 127.0.0.1 |

## 3.2 新增电梯维修

推荐：

| 端口 | 服务 | 公网 |
|---|---|---:|
| `8082` | 电梯维修 Web 管理后台灰度 | ✅ |
| `8083` | 电梯维修 H5 灰度 + 小程序灰度 API | ✅ |
| `8100` | 电梯维修 FastAPI | ❌，仅 127.0.0.1 |
| `3306` | 仍使用 MySQL80 | ❌ |

因此：

```text
萌宠：
http://SERVER_IP/
http://SERVER_IP:8081/

电梯：
http://SERVER_IP:8082/
http://SERVER_IP:8083/
```

两个项目不会抢端口。

---

# 4. 目录必须完全隔离

## 4.1 萌宠商城

保持：

```text
C:\PetLifeMall
```

不要移动、不要覆盖。

## 4.2 电梯维修

新建：

```text
C:\ElevatorRepair
```

推荐完整结构：

```text
C:\ElevatorRepair
│
├─ backend\
│   ├─ app\
│   ├─ migrations\
│   ├─ alembic.ini
│   ├─ pyproject.toml
│   ├─ .venv\
│   └─ .env.local
│
├─ admin-web\
│   ├─ index.html
│   └─ assets\
│
├─ miniprogram-h5\
│   ├─ index.html
│   └─ assets\
│
├─ database\
│   └─ elevator_repair_prod.sql
│
├─ runtime\
│   └─ nginx\
│       ├─ nginx.exe
│       ├─ conf\
│       │   └─ nginx.conf
│       └─ logs\
│
├─ logs\
│
├─ start-api.cmd
├─ start-nginx.cmd
└─ DEPLOYMENT_INFO.md
```

服务器发布包也独立：

```text
C:\packages\ElevatorRepair\
```

备份独立：

```text
C:\Backups\ElevatorRepair\
```

以后删除电梯维修项目时，目录边界非常清楚。

---

# 5. 强烈建议：电梯维修使用独立 Nginx

为了最大程度保证萌宠项目不受影响：

> **不要把电梯维修 server block 继续塞进萌宠正在使用的 `C:\nginx\conf\nginx.conf`。**

推荐给电梯维修运行第二个 Nginx 实例：

```text
萌宠：
C:\nginx\nginx.exe

电梯：
C:\ElevatorRepair\runtime\nginx\nginx.exe
```

两个 Nginx 监听不同端口：

```text
萌宠 Nginx：
80
8081

电梯 Nginx：
8082
8083
```

这样未来删除电梯项目时：

```text
停止 ElevatorRepair-Nginx
删除 C:\ElevatorRepair
```

即可。

萌宠 Nginx 完全不用 reload。

---

# 6. 本地电梯维修项目构建策略

从项目截图可以看到电梯项目本身已经按独立目录组织了：

```text
admin-web
backend
miniprogram
release
deploy
doc
...
```

这非常适合直接套用萌宠项目部署方式。

下面假设：

```text
LOCAL_ELEVATOR=<你的电梯维修项目根目录>
```

例如：

```text
...\bishe27\小程序\电梯维修
```

---

# 7. 电梯管理后台：永久使用相对 API

建议和萌宠项目采用完全相同的设计：

```text
VITE_ADMIN_API_BASE=/api/v1/admin
```

本地 Windows CMD：

```cmd
cd /d <LOCAL_ELEVATOR>\admin-web

set "VITE_ADMIN_API_BASE=/api/v1/admin"

npm.cmd run build
```

构建后：

```text
admin-web\dist\
├─ index.html
└─ assets\
```

上传到服务器：

```text
C:\ElevatorRepair\admin-web\
```

灰度：

```text
http://SERVER_IP:8082
```

后台请求会自动成为：

```text
http://SERVER_IP:8082/api/v1/admin/...
```

正式以后如果改成 HTTPS 域名，同一份 dist 仍然可以继续用。

---

# 8. 电梯 H5：同样使用相对 API

本地 CMD：

```cmd
cd /d <LOCAL_ELEVATOR>\miniprogram

set "VITE_API_BASE_URL=/api/v1"

pnpm.cmd build:h5
```

上传：

```text
C:\ElevatorRepair\miniprogram-h5\
```

灰度访问：

```text
http://SERVER_IP:8083
```

H5 请求：

```text
/api/v1/...
```

浏览器自动变成：

```text
http://SERVER_IP:8083/api/v1/...
```

再由电梯 Nginx 转发到：

```text
127.0.0.1:8100
```

---

# 9. 微信小程序灰度构建

微信小程序不能使用纯 `/api/v1` 作为独立服务器地址。

备案/SSL 未完成期间，在微信开发者工具中：

```cmd
cd /d <LOCAL_ELEVATOR>\miniprogram

set "VITE_API_BASE_URL=http://SERVER_IP:8083/api/v1"

pnpm.cmd build:mp-weixin
```

开发者工具导入：

```text
dist\build\mp-weixin
```

开发期间可开启：

```text
不校验合法域名、TLS版本及 HTTPS 证书
```

正式上线后再切换：

```cmd
set "VITE_API_BASE_URL=https://api.<电梯正式域名>/api/v1"

pnpm.cmd build:mp-weixin
```

---

# 10. 电梯服务器发布包

推荐单独构建：

```text
elevator-repair-server-YYYYMMDD-01.tar.gz
```

内容：

```text
backend/
admin-web/
miniprogram-h5/
database/
```

不要包含：

```text
.git
.venv
node_modules
.env.local
__pycache__
*.pyc
```

服务器上传到：

```text
C:\packages\ElevatorRepair\
```

然后解压到：

```text
C:\ElevatorRepair\
```

---

# 11. Python：只共享 Python 安装，不共享 venv

可以复用服务器已有：

```text
C:\Program Files\Python312\python.exe
```

但必须给电梯项目创建自己的虚拟环境：

```powershell
cd C:\ElevatorRepair\backend

& "C:\Program Files\Python312\python.exe" -m venv .venv

.\.venv\Scripts\python.exe -m pip install --upgrade pip

.\.venv\Scripts\python.exe -m pip install `
  . `
  -i https://mirrors.cloud.tencent.com/pypi/simple `
  --prefer-binary
```

验证：

```powershell
.\.venv\Scripts\python.exe -c "import fastapi, sqlalchemy, pymysql; print('Elevator python dependencies OK')"
```

绝对不要让两个项目共用：

```text
C:\PetLifeMall\backend\.venv
```

---

# 12. 数据库隔离方案

## 12.1 创建独立数据库

进入服务器 MySQL：

```powershell
& "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" -u root -p
```

执行：

```sql
CREATE DATABASE elevator_repair_prod
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_0900_ai_ci;
```

## 12.2 创建独立数据库用户

推荐：

```sql
CREATE USER 'elevator_repair_app'@'127.0.0.1'
IDENTIFIED BY 'REPLACE_WITH_STRONG_PASSWORD';

GRANT ALL PRIVILEGES ON elevator_repair_prod.*
TO 'elevator_repair_app'@'127.0.0.1';

FLUSH PRIVILEGES;
```

检查：

```sql
SHOW GRANTS FOR 'elevator_repair_app'@'127.0.0.1';
```

电梯账号只能操作：

```text
elevator_repair_prod.*
```

不能访问：

```text
pet_life_prod.*
```

这就是数据库层隔离。

---

# 13. 导入电梯项目数据库

本地导出：

```cmd
mysqldump.exe -h 127.0.0.1 -P 3306 -u root -p --single-transaction --routines --triggers --set-gtid-purged=OFF --default-character-set=utf8mb4 <电梯本地数据库名> > elevator_repair_prod.sql
```

必须检查：

```cmd
for %I in ("elevator_repair_prod.sql") do @echo SQL SIZE=%~zI

find /c "CREATE TABLE" "elevator_repair_prod.sql"

findstr /i /c:"Dump completed" "elevator_repair_prod.sql"
```

上传：

```text
C:\ElevatorRepair\database\elevator_repair_prod.sql
```

导入：

```sql
USE elevator_repair_prod;

SOURCE C:/ElevatorRepair/database/elevator_repair_prod.sql;
```

验证：

```sql
SHOW TABLES;
SELECT * FROM alembic_version;
```

---

# 14. 电梯 `.env.local`

独立创建：

```text
C:\ElevatorRepair\backend\.env.local
```

推荐：

```dotenv
APP_ENV=production
APP_NAME=电梯维修 API

DATABASE_URL=mysql+pymysql://elevator_repair_app:URL_ENCODED_PASSWORD@127.0.0.1:3306/elevator_repair_prod?charset=utf8mb4

JWT_SECRET=REPLACE_WITH_ELEVATOR_OWN_RANDOM_SECRET
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=720
```

电梯必须使用自己的：

```text
JWT_SECRET
DATABASE_URL
数据库账号
```

不要复用萌宠 `.env.local`。

---

# 15. 电梯 FastAPI 独立端口

萌宠：

```text
127.0.0.1:8000
```

电梯：

```text
127.0.0.1:8100
```

启动测试：

```powershell
cd C:\ElevatorRepair\backend

.\.venv\Scripts\python.exe -m uvicorn app.main:app `
  --host 127.0.0.1 `
  --port 8100 `
  --workers 1
```

健康检查：

```powershell
Invoke-RestMethod http://127.0.0.1:8100/health
```

如果电梯项目健康检查路径不同，以实际接口为准。

检查两个项目同时存在：

```powershell
Get-NetTCPConnection -LocalPort 8000,8100 -State Listen |
  Select-Object LocalAddress,LocalPort,State,OwningProcess
```

应看到：

```text
127.0.0.1:8000 → PetLifeMall
127.0.0.1:8100 → ElevatorRepair
```

---

# 16. 电梯独立 Nginx 配置

建议把一个干净的 Windows Nginx 解压到：

```text
C:\ElevatorRepair\runtime\nginx
```

配置：

```text
C:\ElevatorRepair\runtime\nginx\conf\nginx.conf
```

内容：

```nginx
worker_processes 1;

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile      on;
    keepalive_timeout 65;

    # ==========================================
    # 电梯 Web 管理后台灰度
    # http://SERVER_IP:8082
    # ==========================================
    server {
        listen 8082;
        server_name _;

        root C:/ElevatorRepair/admin-web;
        index index.html;

        location /api/ {
            proxy_pass http://127.0.0.1:8100;
            proxy_http_version 1.1;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }
    }

    # ==========================================
    # 电梯 H5 / 小程序灰度 API
    # http://SERVER_IP:8083
    # ==========================================
    server {
        listen 8083;
        server_name _;

        root C:/ElevatorRepair/miniprogram-h5;
        index index.html;

        location /api/ {
            proxy_pass http://127.0.0.1:8100;
            proxy_http_version 1.1;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
```

检查：

```powershell
cd C:\ElevatorRepair\runtime\nginx

.\nginx.exe -t
```

启动：

```powershell
Start-Process `
  -FilePath "C:\ElevatorRepair\runtime\nginx\nginx.exe" `
  -WorkingDirectory "C:\ElevatorRepair\runtime\nginx"
```

验证：

```powershell
Get-NetTCPConnection -LocalPort 8082,8083 -State Listen |
  Select-Object LocalAddress,LocalPort,State,OwningProcess
```

---

# 17. 防火墙隔离

## 17.1 Windows Defender

只新增电梯端口：

```powershell
New-NetFirewallRule `
  -DisplayName "ElevatorRepair Admin Gray 8082" `
  -Direction Inbound `
  -Protocol TCP `
  -LocalPort 8082 `
  -Action Allow

New-NetFirewallRule `
  -DisplayName "ElevatorRepair H5 Gray 8083" `
  -Direction Inbound `
  -Protocol TCP `
  -LocalPort 8083 `
  -Action Allow
```

不要开放：

```text
8100
3306
```

## 17.2 腾讯云实例防火墙

新增：

```text
TCP 8082  允许
TCP 8083  允许
```

萌宠已有：

```text
80
8081
```

不要删除或修改。

最终公网端口：

```text
80    → 萌宠管理后台
8081  → 萌宠 H5
8082  → 电梯管理后台
8083  → 电梯 H5 / 小程序灰度 API
```

---

# 18. 两个项目同时灰度测试

## 18.1 萌宠项目

保持：

```text
后台：
http://SERVER_IP

H5：
http://SERVER_IP:8081

FastAPI：
127.0.0.1:8000
```

## 18.2 电梯项目

新增：

```text
后台：
http://SERVER_IP:8082

H5：
http://SERVER_IP:8083

FastAPI：
127.0.0.1:8100
```

## 18.3 验收原则

测试电梯项目时，萌宠以下接口必须仍然正常：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health
```

电梯：

```powershell
Invoke-RestMethod http://127.0.0.1:8100/health
```

同时：

```powershell
Get-NetTCPConnection -LocalPort 80,8081,8082,8083,8000,8100,3306 -ErrorAction SilentlyContinue |
  Select-Object LocalAddress,LocalPort,State,OwningProcess
```

合理结果：

```text
0.0.0.0:80       → 萌宠 Nginx
0.0.0.0:8081     → 萌宠 Nginx

0.0.0.0:8082     → 电梯 Nginx
0.0.0.0:8083     → 电梯 Nginx

127.0.0.1:8000   → 萌宠 FastAPI
127.0.0.1:8100   → 电梯 FastAPI

127.0.0.1:3306   → MySQL80
```

---

# 19. 电梯独立开机启动

## 19.1 FastAPI 启动脚本

```text
C:\ElevatorRepair\start-api.cmd
```

内容：

```bat
@echo off
cd /d C:\ElevatorRepair\backend
C:\ElevatorRepair\backend\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8100 --workers 1
```

任务：

```powershell
schtasks /Create `
  /TN "ElevatorRepair-API" `
  /SC ONSTART `
  /RU SYSTEM `
  /RL HIGHEST `
  /TR "C:\ElevatorRepair\start-api.cmd" `
  /F
```

## 19.2 Nginx 启动脚本

```text
C:\ElevatorRepair\start-nginx.cmd
```

内容：

```bat
@echo off
cd /d C:\ElevatorRepair\runtime\nginx
start "" C:\ElevatorRepair\runtime\nginx\nginx.exe
```

任务：

```powershell
schtasks /Create `
  /TN "ElevatorRepair-Nginx" `
  /SC ONSTART `
  /RU SYSTEM `
  /RL HIGHEST `
  /TR "C:\ElevatorRepair\start-nginx.cmd" `
  /F
```

任务命名带项目名前缀，未来很好识别和删除。

---

# 20. 资源竞争控制

虽然两个项目代码和端口隔离，但仍共享同一台物理服务器的：

```text
CPU
内存
磁盘
网络
MySQL80
```

为了不影响萌宠灰度：

1. 两个 FastAPI 均使用 `--workers 1`。
2. 不使用 `--reload`。
3. 不在服务器执行前端 Node 构建，前端全部在本地构建后上传。
4. 暂时不运行高并发压测。
5. 大文件上传测试错峰进行。
6. 数据库批量初始化、批量导入尽量避免与萌宠测试同时进行。
7. 定期检查：

```powershell
Get-Process python,nginx,mysqld -ErrorAction SilentlyContinue |
  Select-Object Id,ProcessName,CPU,WorkingSet
```

---

# 21. 推荐创建 DEPLOYMENT_INFO.md

在：

```text
C:\ElevatorRepair\DEPLOYMENT_INFO.md
```

记录：

```markdown
# ElevatorRepair Temporary Deployment

FastAPI:
127.0.0.1:8100

Database:
elevator_repair_prod

DB User:
elevator_repair_app@127.0.0.1

Admin Gray:
http://SERVER_IP:8082

H5 Gray:
http://SERVER_IP:8083

Scheduled Tasks:
ElevatorRepair-API
ElevatorRepair-Nginx

Windows Firewall Rules:
ElevatorRepair Admin Gray 8082
ElevatorRepair H5 Gray 8083

Tencent Cloud Firewall:
TCP 8082
TCP 8083

Project Root:
C:\ElevatorRepair

Package Root:
C:\packages\ElevatorRepair
```

注意：**不记录密码和 JWT_SECRET**。

这个文件就是未来迁移/删除清单。

---

# 22. 未来迁移到电梯独立腾讯云服务器

假设后续电梯服务器已经申请成功。

## 22.1 旧服务器导出数据库

```powershell
& "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqldump.exe" `
  -h 127.0.0.1 `
  -P 3306 `
  -u root `
  -p `
  --single-transaction `
  --routines `
  --triggers `
  --set-gtid-purged=OFF `
  --default-character-set=utf8mb4 `
  elevator_repair_prod `
  --result-file="C:\Backups\ElevatorRepair\elevator_repair_prod.sql"
```

检查：

```powershell
Get-Item C:\Backups\ElevatorRepair\elevator_repair_prod.sql |
  Select-Object Length,LastWriteTime
```

## 22.2 复制项目

需要迁移：

```text
C:\ElevatorRepair\
```

其中：

```text
backend
admin-web
miniprogram-h5
runtime\nginx
start-api.cmd
start-nginx.cmd
```

`.env.local` 含敏感信息，必须通过安全方式转移，或者在新服务器重新生成。

## 22.3 新服务器恢复

新服务器：

```text
安装 Python
安装 MySQL
创建 elevator_repair_prod
创建 elevator_repair_app
导入 SQL
创建 backend\.venv
安装依赖
生成 .env.local
启动 8100
启动电梯 Nginx
验收
```

## 22.4 迁移验收

必须先确认新服务器：

```text
FastAPI health OK
后台 OK
H5 OK
数据库数据数量正确
关键业务 CRUD 正常
```

然后再清理旧服务器。

---

# 23. 旧服务器快速删除电梯项目

这是本方案的核心优势。

> 只有确认新服务器完全正常之后才执行。

## 23.1 删除计划任务

```powershell
schtasks /Delete /TN "ElevatorRepair-API" /F
schtasks /Delete /TN "ElevatorRepair-Nginx" /F
```

## 23.2 停止电梯进程

先确认 8100：

```powershell
Get-NetTCPConnection -LocalPort 8100 -State Listen |
  Select-Object OwningProcess
```

停止对应 PID：

```powershell
Stop-Process -Id <ELEVATOR_API_PID> -Force
```

再确认：

```powershell
Get-NetTCPConnection -LocalPort 8082,8083 -State Listen |
  Select-Object OwningProcess
```

只停止属于：

```text
C:\ElevatorRepair\runtime\nginx\nginx.exe
```

的进程。

不要执行：

```powershell
Stop-Process -Name nginx -Force
```

因为这可能把萌宠 Nginx 一起停止。

推荐通过：

```powershell
Get-CimInstance Win32_Process -Filter "Name='nginx.exe'" |
  Select-Object ProcessId,ExecutablePath,CommandLine
```

先确认路径后再停止。

## 23.3 删除 Windows 防火墙规则

```powershell
Remove-NetFirewallRule `
  -DisplayName "ElevatorRepair Admin Gray 8082"

Remove-NetFirewallRule `
  -DisplayName "ElevatorRepair H5 Gray 8083"
```

## 23.4 腾讯云控制台删除

只删除：

```text
TCP 8082
TCP 8083
```

不要动萌宠：

```text
80
8081
```

## 23.5 删除数据库

进入 MySQL：

```sql
DROP DATABASE elevator_repair_prod;

DROP USER 'elevator_repair_app'@'127.0.0.1';

FLUSH PRIVILEGES;
```

不要删除：

```text
pet_life_prod
MySQL80
```

## 23.6 删除目录

确认备份完成后：

```powershell
Remove-Item C:\ElevatorRepair -Recurse -Force
Remove-Item C:\packages\ElevatorRepair -Recurse -Force
```

这样旧服务器中的电梯项目即可完整退出。

---

# 24. 清理后验证萌宠商城不受影响

执行：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health
```

检查：

```powershell
Get-NetTCPConnection -LocalPort 80,8081,8000,3306 -ErrorAction SilentlyContinue |
  Select-Object LocalAddress,LocalPort,State,OwningProcess
```

浏览器：

```text
http://SERVER_IP
http://SERVER_IP:8081
```

必须仍然正常。

数据库：

```sql
SHOW DATABASES;
```

确认：

```text
pet_life_prod
```

仍然存在。

---

# 25. 两个项目的隔离矩阵

| 项目 | 萌宠商城 | 电梯维修 |
|---|---|---|
| 项目目录 | `C:\PetLifeMall` | `C:\ElevatorRepair` |
| Python venv | `PetLifeMall\backend\.venv` | `ElevatorRepair\backend\.venv` |
| FastAPI | `127.0.0.1:8000` | `127.0.0.1:8100` |
| 数据库 | `pet_life_prod` | `elevator_repair_prod` |
| DB 用户 | PetLife 自己 | `elevator_repair_app` |
| 管理后台 | `:80` | `:8082` |
| H5 | `:8081` | `:8083` |
| Nginx | `C:\nginx` | `C:\ElevatorRepair\runtime\nginx` |
| 计划任务 | `PetLifeMall-API` | `ElevatorRepair-API` |
| Nginx任务 | 萌宠自己的 | `ElevatorRepair-Nginx` |
| 发布包 | PetLife 独立 | `C:\packages\ElevatorRepair` |
| JWT | 萌宠独立 | 电梯独立 |
| 日志 | 萌宠独立 | 电梯独立 |

---

# 26. 最终推荐部署顺序

```text
01. 绝对不要修改 C:\PetLifeMall
02. 创建 C:\ElevatorRepair
03. 创建 C:\packages\ElevatorRepair
04. 本地构建电梯 admin-web（相对 /api）
05. 本地构建电梯 H5（相对 /api）
06. 导出电梯本地数据库
07. 打包电梯 backend/admin/H5/database
08. 上传并解压
09. 创建电梯独立 .venv
10. 安装电梯 Python 依赖
11. MySQL 创建 elevator_repair_prod
12. 创建 elevator_repair_app
13. 导入数据库
14. 创建电梯独立 .env.local
15. Alembic 检查/升级
16. FastAPI 启动到 127.0.0.1:8100
17. 验证 /health
18. 部署第二套 Nginx
19. 电梯 Nginx 监听 8082 / 8083
20. Windows 防火墙放行 8082/8083
21. 腾讯云实例防火墙放行 8082/8083
22. 验证 http://IP:8082 后台
23. 验证 http://IP:8083 H5
24. 微信开发者工具按 IP:8083 构建灰度小程序
25. 创建 ElevatorRepair-API 开机任务
26. 创建 ElevatorRepair-Nginx 开机任务
27. 最后再复测萌宠 80 / 8081 / 8000
```

---

# 27. 最终结论

这套方案满足两个核心要求：

## 要求 1：电梯项目可快速迁移、快速删除

因为电梯维修拥有独立：

```text
C:\ElevatorRepair
elevator_repair_prod
elevator_repair_app
8100
8082
8083
独立 Nginx
独立计划任务
独立 Windows 防火墙规则
独立腾讯云防火墙规则
```

未来迁走后只删除这些对象即可。

## 要求 2：不影响萌宠商城灰度

萌宠继续：

```text
80
8081
8000
pet_life_prod
C:\PetLifeMall
C:\nginx
```

电梯使用另一套端口和目录，不修改萌宠项目代码、数据库、虚拟环境和 Nginx 配置。

---

## 一句话架构原则

> **同一台服务器临时承载两个项目没有问题；关键不是“完全不共享任何软件”，而是共享基础运行环境、隔离项目状态：目录隔离、数据库隔离、账号隔离、端口隔离、Nginx隔离、任务隔离、日志隔离。这样既能快速上线灰度，也能随时把电梯维修项目整体迁走。**

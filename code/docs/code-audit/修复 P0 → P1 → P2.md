# Codex 第二阶段任务：基于代码审计结果修复 P0 → P1 → P2

项目根目录：

E:\bishe27\服务平台小程序\code

已经完成第一阶段只读代码审计。

审计文档位于：

docs/code-audit/

重点包括：

- TEST_CASE_AUDIT.md
- TEST_CASE_MATRIX.md
- CODE_GAP_REPORT.md
- PROJECT_READINESS_SUMMARY.md
- ORDER_STATE_MACHINE_AUDIT.md
- ROLE_PERMISSION_MATRIX.md
- DATABASE_MATRIX.md
- LOCAL_VS_PRODUCTION.md

本阶段开始修改代码。

---

# 一、总原则

严格按照：

P0
↓
测试
↓
P1
↓
测试
↓
P2

执行。

禁止：

- 一次性大规模重构
- 改变整个架构
- 删除现有安全能力
- 把生产逻辑改成 Mock
- 为了通过测试而硬编码
- 修改已有正确订单状态机
- 随意修改数据库字段
- 将密码明文保存
- 把测试 helper 直接复制到生产

每完成一个阶段必须运行：

npm test

并记录测试结果。

---

# 二、先校正当前审计中的两个评级

开始修复之前先重新确认：

## TC29

当前 TEST_CASE_MATRIX 将钱包提现标记为：

PASS_CODE

但 CODE_GAP_REPORT 已发现：

minWithdrawalFen 可以保存，
applyWithdrawal 却没有真正读取配置。

因此重新确认。

如果测试要求：

管理员修改起提金额
↓
新配置立即影响提现业务

而当前代码未做到：

TC29 应调整为 PARTIAL。

---

## TC30

当前报告因为 Admin 支持：

SUPER_ADMIN
BRAND_ADMIN
FINANCE_REVIEWER
ARBITRATOR

而标记 REQUIREMENT_MISMATCH。

但测试需求实际上主要要求：

CS 不允许登录 Admin
WORKER 不允许登录 Admin

请检查角色模型。

如果上述角色属于管理后台内部正式管理角色：

不要删除这些角色。

只需要确保：

CS
WORKER
CUSTOMER

不能登录 /admin/。

将分析写回 TEST_CASE_MATRIX。

---

# 三、P0-01：修复订单留言对象级权限

涉及：

TC22

重点搜索：

sendOrderMessage
listOrderMessages
order_messages
orders
canAccessBrand
session.userId
workerId

当前风险：

同品牌 WORKER 只需要知道其他订单 orderId，
可能读取或者发送其他订单留言。

必须实现统一对象级授权。

## WORKER

发送和读取消息前：

1. 根据 orderId 加载订单
2. 订单必须存在
3. 校验：

order.workerId === session.userId

否则：

FORBIDDEN

不允许：

WORKER A
访问
WORKER B 的订单消息。

## CS / 管理角色

仍然必须：

检查订单 brandId
↓
检查 session.brandScopes

不能因为是后台角色就跨品牌访问。

## CUSTOMER

如果 CUSTOMER 存在订单消息访问入口：

必须验证订单确实属于该客户。

不要根据前端隐藏按钮判断权限。

权限必须在 game-service 后端执行。

---

## 自动化测试

增加：

1. Worker A 读取自己的订单消息 → PASS
2. Worker A 给自己的订单留言 → PASS
3. Worker A 读取 Worker B 订单 → FORBIDDEN
4. Worker A 给 Worker B 订单留言 → FORBIDDEN
5. Brand A CS 访问 Brand B 订单 → FORBIDDEN
6. 平台超级管理员按照现有设计验证允许范围

P0-01 修复后：

npm test

所有现有测试和新增测试必须通过。

---

# 四、P0-02：修复报表跨品牌数据泄露

涉及：

TC38

搜索全部：

report
profit
withdrawal report
performance
wallet report
order report

当前问题：

部分 report action 直接读取全部：

orders
users
withdrawals
wallet_transactions

没有按照：

session.brandScopes

统一过滤。

---

## 实现要求

增加统一品牌范围过滤策略。

如果：

session.brandScopes = ['brand-a']

则所有报表：

只能返回 brand-a。

如果：

session.brandScopes = ['brand-a','brand-b']

只返回 A+B。

如果：

session.brandScopes = ['*']

才允许平台级全量报表。

如果调用参数传：

brandId

还必须验证：

brandId 是否包含在 session.brandScopes。

禁止：

用户通过修改请求中的 brandId
绕过品牌权限。

---

## 特别检查资金数据

必须检查：

orders
wallet_transactions
withdrawals
users
worker_profiles

这些集合如何关联 brandId。

如果某资金记录没有直接 brandId：

必须通过可靠业务关系过滤，

不能因为过滤困难就返回全部。

---

## 测试

创建：

Brand A
Brand B

各自：

订单
Worker
钱包
提现

验证：

Brand A 管理员
→ 只能看 Brand A

Brand B 管理员
→ 只能看 Brand B

平台管理员 *
→ 可以看全部

P0-02 修复以后：

npm test

---

# 五、P0 阶段 Gate

两个 P0 修复完成后：

必须生成：

docs/code-audit/P0_FIX_REPORT.md

内容：

- 修改文件
- 修改函数
- 新增测试
- npm test 结果
- TC22 新评级
- TC38 新评级

如果任何 P0 自动化测试失败：

停止。

不要进入 P1。

---

# 六、P1-A：让后台配置真正控制业务

当前重点：

poolTimeoutMinutes
assignmentTimeoutMinutes
minWithdrawalFen

以及其他配置项。

审计发现：

配置页面可以保存，
但部分业务仍读取：

DEFAULTS
硬编码
订单旧字段

必须修正。

---

## 建立统一配置读取

优先复用现有：

getConfigs
config service
repository

不要新增第二套配置系统。

业务执行时应该：

数据库配置
↓
业务默认值 fallback

即：

const config = 数据库配置 ?? DEFAULTS

而不是永远使用 DEFAULTS。

---

## 重点检查

### poolTimeoutMinutes

必须影响：

抢单池超时标记。

前端不得硬编码：

30 分钟。

---

### assignmentTimeoutMinutes

必须影响：

指派超时自动拒绝。

system-runner 必须读取同一配置。

---

### minWithdrawalFen

必须影响：

applyWithdrawal

管理员修改：

1000
→ 2000

以后：

15 元提现应该失败。

不能继续使用领域默认 10 元。

---

## 顺带扫描全部配置

检查：

同时接单上限
周提现次数
补单次数
支付超时
异议窗口

是否也存在：

保存成功
但业务仍读硬编码

如果发现，加入同一修复。

---

# 七、P1-B：system-runner 定时任务

当前存在：

system-runner

但仓库无法证明定时触发器已定义。

请检查：

package.json
config
uniCloud trigger 配置
部署脚本

需要建立可部署的定时任务定义。

至少支持：

支付超时关闭
assignment 超时
pool timeout mark

不要在代码中假装已经部署。

如果 DCloud 定时触发器不能完全由仓库定义：

生成：

docs/deployment/SYSTEM_RUNNER_TRIGGER.md

明确：

- 函数名称
- cron
- 调用 action
- 参数
- 阿里云 uniCloud 控制台操作
- 验证方法

并把测试项保留为：

MANUAL_TEST_REQUIRED

直到真实部署验证。

---

# 八、P1-C：员工管理

TC31 要求 Admin：

- 创建 Worker
- 创建 CS
- 停用员工
- 实名通过
- 实名驳回

当前前端主要支持 Worker。

请复用已有：

createStaff
updateStaff

增加：

创建客服

不要创建第二套 employee API。

实名审核增加：

APPROVE
REJECT

驳回时允许填写原因。

---

# 九、P1-D：VIP 参数不一致

当前前端 removeVip：

发送：

matchType
matchKey

后端：

要求：

vipId

必须统一。

优先原则：

选用最稳定的唯一 ID：

vipId

推荐：

listVips 返回 vipId
↓
前端删除时传 vipId
↓
removeVip(vipId)

不要后端同时无限兼容多套旧协议，
除非已有兼容需求。

增加删除测试。

---

# 十、P1-E：字典

TC33 要求：

新增
停用
引用保护

需要实现：

ACTIVE
DISABLED

被历史订单引用的字典：

不能物理删除。

优先：

软删除 / disable

禁止：

删除后历史订单无法解释。

---

# 十一、P1-F：红线词统一

当前 Admin 前端和生产 game-service 的词表不一致。

必须建立：

单一红线词来源

避免：

前端一套
后端一套

生产后端必须至少覆盖测试文档要求的全部红线词。

注意：

真正安全边界在后端。

即使前端绕过：

saveProduct

仍然必须拒绝违规词。

新增后端测试。

---

# 十二、P1-G：数据库唯一索引

审计指出：

42 个 schema 中无法证明关键唯一索引已经部署。

重点确认：

orderNo
payment transaction id
idempotencyKey
phone blind index
withdrawal id
refund id
其他业务唯一键

生成：

docs/deployment/UNICLOUD_INDEXES.md

格式：

Collection
Field
Index Type
Unique
Purpose

如果 uniCloud schema 支持声明：

补充代码。

如果必须云控制台创建：

给出逐项操作清单。

不要仅依赖：

find()
→ 没找到
→ insert()

来保证唯一性。

---

# 十三、P2 修复

P0/P1 全部测试通过后再处理。

包括：

TC01
下架商品通过 getProduct(id) 仍可能读取。

要求：

客户端公开 getProduct
必须检查：

status === ON

后台管理查询使用独立管理 action。

---

TC06：

支付成功页增加：

联系客服

并自动携带：

订单号。

---

TC11：

客服 Dashboard 待办数量建立统一口径。

不要：

页面自己定义一套状态。

优先以后端 dashboard action 为唯一来源。

---

TC23：

统一首次改密规则。

当前：

前端：
至少 8 位 + 字母数字

后端：
12~128 位 + 四类至少三类

必须统一。

以前端提示严格匹配后端规则。

---

TC24：

同时进行中订单数量：

不能写死：

3

应该读取后台配置。

新订单提醒检查：

声音
角标

没有实现再补。

---

# 十四、不要修改这些内容

本阶段不要处理：

微信真实支付
微信 OAuth
订阅消息真实送达
真实退款回调

这些属于：

BLOCKED_BY_EXTERNAL_SERVICE

保持现有生产代码，
只记录人工验收步骤。

---

# 十五、Admin 简化登录保持兼容

当前项目部署联调阶段允许：

ADMIN_SIMPLE_LOGIN=true

如果该配置已经存在：

不要删除。

简化模式仅影响 ADMIN 登录安全策略。

仍然必须保留：

- users 查询
- password hash
- ADMIN/管理角色权限判断
- session/token
- PII
- 后端授权

禁止：

if phone == 13800000000
直接登录

CS / WORKER 的登录业务仍按照测试文档执行。

---

# 十六、每阶段测试

每完成一项：

运行针对性测试。

每完成一个优先级：

运行：

npm test

当前基线是：

192 tests passing

修改后不能低于基线，
除非原测试本身被证明与需求冲突。

如果调整已有测试：

必须在报告解释原因。

---

# 十七、最终重新生成审计报告

全部完成以后重新生成：

docs/code-audit/TEST_CASE_MATRIX_V2.md
docs/code-audit/CODE_GAP_REPORT_V2.md
docs/code-audit/PROJECT_READINESS_SUMMARY_V2.md
docs/code-audit/FIX_IMPLEMENTATION_REPORT.md

重新计算：

PASS_CODE
PARTIAL
REQUIREMENT_MISMATCH
SECURITY_RISK
MANUAL_TEST_REQUIRED
BLOCKED_BY_EXTERNAL_SERVICE

目标：

SECURITY_RISK = 0

MISSING = 0

P0 = 0

---

# 十八、不要伪造 Production Ready

即使自动化全部通过：

以下项目仍不能自动标记 PASS：

- 微信 JSAPI
- 微信 H5 支付
- 微信支付回调
- 退款回调
- 订阅消息
- CDN/域名
- 手机浏览器
- 定时触发器真实运行

必须保留：

MANUAL_TEST_REQUIRED

或：

BLOCKED_BY_EXTERNAL_SERVICE

直到真实环境验证。

---

# 十九、最终终端输出

==========================================
PROJECT FIX SUMMARY
==========================================

P0 fixed:
P1 fixed:
P2 fixed:

Tests before:
192

Tests after:

SECURITY_RISK:
REQUIREMENT_MISMATCH:
PARTIAL:
PASS_CODE:

Files changed:

Manual validation remaining:

Reports:
docs/code-audit/*_V2.md

==========================================

完成以后停止。

不要继续修改部署环境，
不要自动上传 uniCloud，
不要自动发布 H5。

等待我审核。
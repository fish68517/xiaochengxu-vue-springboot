# 多品牌服务平台部署文档

> 文档适用目录：`E:\bishe27\服务平台小程序\code`  
> 文档生成日期：2026-08-31  
> 适用范围：客户小程序、客户 H5、客服/接单工作台、管理后台、uniCloud 云函数、uniCloud 数据库、微信支付、公众号网页授权、微信客服回调、订阅消息、发布验证与回滚。

## 1. 部署结论与边界

本项目不是传统的“独立 Spring Boot/Node 服务 + MySQL”结构，正式后端由 uniCloud 云函数和 uniCloud 数据库承担；`packages/backend` 与 `scripts/api-server.mjs` 是本地联调、自动化测试所使用的镜像后端，不能作为生产数据库服务直接上线。

正式部署包含以下部分：

| 部分 | 代码位置 | 部署目标 | 主要入口 |
|---|---|---|---|
| 客户小程序 | `code/apps/client` | 微信小程序 | 微信开发者工具上传、微信公众平台发布 |
| 客户 H5 | `code/apps/client` | 客户域静态托管 | `/`，实际页面使用 hash 路由 |
| 客服/接单工作台 | `code/apps/workbench` | 后台域静态托管 | `/workbench/` |
| 管理后台 | `code/apps/admin` | 后台域静态托管 | `/admin/` |
| 主业务后端 | `code/uniCloud-tcb/cloudfunctions/game-service` | uniCloud 云函数 | `game-service` action 路由；支付回调 `/pay-notify` |
| 微信客服回调 | `code/uniCloud-tcb/cloudfunctions/wechat-callback` | uniCloud 云函数 | 微信消息推送与客服链接下发 |
| 数据库 | `code/uniCloud-tcb/database` | uniCloud 数据库 | 26 个 schema 和 16 个索引定义 |
| 领域逻辑单一源 | `code/packages/domain` | 构建进主云函数 | 生成 `game-service/lib/domain.cjs` |
| 本地镜像后端 | `code/packages/backend` | 只用于开发/测试 | 内存数据库，进程重启后数据丢失 |

推荐将正式环境划分为三个独立环境，至少保证 staging 与 production 使用不同的 uniCloud 空间、数据库、域名、微信配置和密钥：

| 环境 | `API_MODE` | `PAYMENT_MODE` | `MESSAGE_MODE` | 用途 |
|---|---|---|---|---|
| development | `local` | `mock` | `mock` | 本地开发与自动化测试 |
| staging | `unicloud` | `wechat` | `wechat` | 真实微信、真实云空间的上线前验收 |
| production | `unicloud` | `wechat` | `wechat` | 正式业务 |

当前代码已通过本地 `verify:all` 的 8 项检查，但这只证明本地 lint、测试、构建和 Mock 冒烟通过，不代表真实云空间、真实支付、真实退款、真实小程序审核已经通过。正式发布前必须完成本文的 staging 验收。

## 2. 推荐生产拓扑

建议准备三个 HTTPS 域名或子域名：

| 示例域名 | 用途 | 部署内容 |
|---|---|---|
| `https://h5.example.com` | 客户 H5/web-view | `apps/client/dist/build/h5` |
| `https://admin.example.com` | 客服、接单、管理后台 | `/workbench/` 和 `/admin/` |
| `https://api.example.com` | 微信服务器回调 | `/pay-notify` 与微信客服回调路径 |

域名示例必须替换为实际已备案、已配置 HTTPS 证书的域名。客户 H5 与后台 H5 使用 hash 路由，因此通常不需要服务器端 history fallback；但 `/workbench/` 和 `/admin/` 两个目录前缀必须与构建配置保持一致。

小程序通过 `uniCloud.callFunction('game-service')` 调用云函数。H5 构建在未设置 `VITE_API_BASE` 时优先使用运行时的 `uniCloud.callFunction`；本地测试通过 `VITE_API_BASE=http://127.0.0.1:4176` 调用镜像 HTTP API。

重要上线检查：当前自动化 UI 冒烟使用的是本地 HTTP API。staging 必须在真实静态托管页面中确认 `uniCloud.callFunction` 可用。如果浏览器中出现“uniCloud 不可用”或“未配置后端地址”，说明 H5 与云空间未正确关联；在解决云空间关联或补齐正式 HTTP API 网关之前，不得发布生产。支付回调的 URL 化入口不能替代完整 H5 API 网关。

## 3. 部署前准备

### 3.1 软件与账号

发布机建议准备：

- Windows 10/11；
- Node.js 20 LTS 或 22 LTS，且 `node`、`npm` 可在 PowerShell 中执行；
- HBuilderX 正式版及其 CLI；
- 微信开发者工具；
- 已实名认证并开通云函数、云数据库和静态托管的 uniCloud 账号；
- 已认证的微信小程序、服务号和微信支付商户号；
- 已备案域名及有效 HTTPS 证书；
- 对 uniCloud 空间、微信公众平台、微信支付商户平台具有发布权限的账号。

检查基础环境：

```powershell
node --version
npm --version
& 'F:\HBuilderX\cli.exe' --version
```

如果 HBuilderX 不在 `F:\HBuilderX`，后续通过 `HX_CLI` 指定实际路径。

### 3.2 进入代码目录并安装依赖

所有构建、验证和部署命令都必须在 `code` 目录执行：

```powershell
Set-Location -LiteralPath 'E:\bishe27\服务平台小程序\code'

npm install
npm install --prefix apps/client
npm install --prefix apps/workbench
npm install --prefix apps/admin
```

生产发布建议使用仓库锁文件安装：

```powershell
npm ci
npm ci --prefix apps/client
npm ci --prefix apps/workbench
npm ci --prefix apps/admin
```

不要删除或手工修改 `package-lock.json` 后直接发布。

### 3.3 准备云空间

建议至少创建两个空间：

- staging 空间：验收、支付小额实测、迁移演练；
- production 空间：正式数据和正式密钥。

如客户 H5 与后台 H5需要分别绑定不同域名或隔离权限，可再为静态托管准备独立空间。部署脚本支持：

- `UNICLOUD_SPACE`：云函数和数据库空间；
- `HOSTING_CLIENT_SPACE`：客户 H5 静态托管空间；
- `HOSTING_BACKEND_SPACE`：后台 H5 静态托管空间。

查看 HBuilderX 当前账号可用空间：

```powershell
$env:HX_CLI = 'F:\HBuilderX\cli.exe'
$env:HX_PROJECT = 'client'
node scripts/list-unicloud.mjs
```

脚本会同时列出 `tcb` 和 `aliyun` 空间。`UNICLOUD_PROVIDER` 必须明确设置为实际 CLI 支持的 `tcb` 或 `aliyun`，不要依赖默认值，也不要把 `.env.example` 中的示例值直接当作正式配置。

## 4. 环境变量与密钥

### 4.1 配置原则

`code/.env.example` 只是键名合同，脚本不会自动读取该文件。执行本地命令时应由 PowerShell、CI/CD 或发布系统注入环境变量；云函数运行时变量应在 uniCloud 控制台的云函数配置/安全配置中设置。

严禁把以下内容提交到 Git、`config/brands/*.env`、前端构建产物或聊天记录：

- 商户私钥；
- APIv3 密钥；
- 小程序和公众号 Secret；
- `SESSION_SECRET`、`H5_TOKEN_SECRET`、`INTERNAL_SECRET`；
- 数据库导出、真实用户手机号、openid、订单和资金数据。

`VITE_*` 变量会进入前端产物，只允许放公开信息。

### 4.2 发布与托管变量

| 变量 | 必填环境 | 说明 |
|---|---|---|
| `APP_ENV` | 全部 | `development`、`staging`、`production` |
| `API_MODE` | 全部 | 本地为 `local`，远端为 `unicloud` |
| `PAYMENT_MODE` | 全部 | 本地为 `mock`，远端为 `wechat` |
| `MESSAGE_MODE` | 全部 | 本地为 `mock`，远端为 `wechat` |
| `BRAND_CODE` | 全部 | 当前发布品牌 code，如 `demo-a` |
| `HX_CLI` | 发布机 | HBuilderX CLI 的绝对路径 |
| `HX_PROJECT` | 发布机 | HBuilderX 项目名，当前脚本默认 `client` |
| `UNICLOUD_PROVIDER` | staging/production | 明确设为 `tcb` 或 `aliyun` |
| `UNICLOUD_SPACE` | staging/production | 云函数及数据库目标空间名称或 ID |
| `HOSTING_CLIENT_SPACE` | staging/production | 客户 H5 托管空间；为空时脚本回退到 `UNICLOUD_SPACE` |
| `HOSTING_BACKEND_SPACE` | staging/production | 后台 H5 托管空间；为空时脚本回退到 `UNICLOUD_SPACE` |
| `HOSTING_CLIENT_URL` | staging/production | 客户 H5 HTTPS 根地址，不带结尾 `/` |
| `HOSTING_BACKEND_URL` | staging/production | 后台 HTTPS 根地址，不带结尾 `/` |

### 4.3 云函数会话与内部调用密钥

| 变量 | 配置到 | 说明 |
|---|---|---|
| `SESSION_SECRET` | `game-service` | 登录会话签名密钥；缺失时拒绝服务 |
| `H5_TOKEN_SECRET` | `game-service` | H5 下单 token 签名密钥；缺失时拒绝服务 |
| `INTERNAL_SECRET` | 两个云函数，值必须相同 | `wechat-callback` 调用 `game-service` 的系统间密钥 |
| `H5_TOKEN_TTL_MS` | `game-service` | H5 token 有效期，默认 86400000 毫秒，即 24 小时 |
| `H5_ORDER_BASE_URL` | 两个云函数 | 客户 H5 根地址，例如 `https://h5.example.com` |

密钥应使用高强度随机值，各环境不同，并建立轮换记录。不要使用文档里的示例文字作为真实密钥。

### 4.4 微信账号变量

| 变量 | 配置到 | 说明 |
|---|---|---|
| `OAUTH_APPID` | `game-service` | 认证服务号 AppID，用于 H5 `snsapi_base` 网页授权；与前端 `VITE_OAUTH_APPID` 为同一值 |
| `OAUTH_SECRET` | `game-service` | 服务号 AppSecret；只配置在云函数服务端，不写入前端或仓库 |
| `WECHAT_MP_APPID` | 两个云函数 | 当前品牌小程序 AppID |
| `WECHAT_MP_SECRET` | 两个云函数 | 当前品牌小程序 Secret |
| `WECHAT_MP_KF_TOKEN` | `wechat-callback` | 微信消息回调验签 token |
| `WECHAT_MP_TEMPLATE_IDS` | `game-service` | 订阅消息模板映射 JSON，例如 `{"ORDER_STATUS":"模板ID"}` |

如果同一个云函数空间服务多个品牌，而不同品牌使用不同小程序 AppID/Secret，必须确认运行时按 `brandId` 读取品牌的 `secretRef`。当前环境变量形式是单组微信账号配置，生产多品牌不能让后发布品牌覆盖先前品牌的密钥；应在 staging 验证多品牌密钥路由，未实现时需要按品牌拆分空间或先补齐 Secret 引用解析。

### 4.5 微信支付变量

| 变量 | 说明 |
|---|---|
| `WECHAT_PAY_MCHID` | 微信支付商户号 |
| `WECHAT_PAY_SERIAL_NO` | 商户 API 证书序列号 |
| `WECHAT_PAY_PRIVATE_KEY` | `apiclient_key.pem` 内容，单行保存时支持 `\n` 转义 |
| `WECHAT_PAY_APIV3_KEY` | APIv3 密钥 |
| `WECHAT_PAY_APPID` | 已与商户号绑定的支付 AppID |
| `WECHAT_PAY_NOTIFY_URL` | 完整 HTTPS 回调地址，例如 `https://api.example.com/pay-notify` |

上述变量配置到 `game-service`。一个 `WECHAT_PAY_APPID` 只能代表当前运行时的一组支付配置；多品牌若使用不同商户号或 AppID，必须使用品牌 `secretRef` 对应的多租户支付配置，或将品牌拆分到不同云空间。不要仅修改前端 AppID 就认为支付配置已经完成隔离。

### 4.6 前端公开变量

| 变量 | 用途 |
|---|---|
| `VITE_API_BASE` | 本地联调 API 地址；正式 H5 走 uniCloud 时留空 |
| `VITE_H5_ORDER_BASE_URL` | 小程序 web-view 打开的客户 H5 根地址 |
| `VITE_OAUTH_APPID` | 服务号网页授权 AppID |
| `VITE_BRAND_CODE` | 品牌 code |
| `VITE_BRAND_DISPLAY_NAME` | 品牌展示名 |
| `VITE_WECHAT_APP_ID` | 品牌小程序 AppID，非 Secret |
| `VITE_PUBLIC_CHANNEL_ID` | 品牌公开渠道标识 |

品牌公开配置位于 `code/config/brands/<brand>.env`。构建脚本会拒绝包含 `SECRET`、`PRIVATE`、`PASSWORD`、`API_KEY`、`MCH_KEY` 等敏感键的品牌文件。

### 4.7 运维变量

| 变量 | 说明 |
|---|---|
| `PORT` | 本地镜像 API 端口，默认 `4176` |
| `BACKUP_DIR` | 本地备份/演练目录，默认 `./backups` |
| `ALERT_LOG_DIR` | 告警留档目录，默认 `./alerts` |
| `ALERT_WEBHOOK_URL` | 告警 webhook；当前是预留通道，需真实接收端验证 |
| `RELEASE_APPROVED` | production 门禁，必须是 `true` |
| `DB_BACKUP_ID` | production 发布对应的真实数据库备份 ID |
| `ROLLBACK_BUNDLE_PATH` | 已审核的回滚清单绝对路径 |

## 5. 本地部署与联调

### 5.1 启动镜像后端

本地 API 使用内存数据库，会自动写入管理员、客服、接单员、两个演示品牌、商品和订单数据：

```powershell
Set-Location -LiteralPath 'E:\bishe27\服务平台小程序\code'
$env:APP_ENV = 'development'
$env:API_MODE = 'local'
$env:PAYMENT_MODE = 'mock'
$env:MESSAGE_MODE = 'mock'
$env:SESSION_SECRET = '仅用于本机开发的随机值'
$env:H5_TOKEN_SECRET = '仅用于本机开发的另一随机值'
$env:PORT = '4176'
node scripts/api-server.mjs
```

本地演示账号：

| 角色 | 手机号 | 密码 |
|---|---|---|
| 管理员 | `13800000000` | `admin123` |
| 客服 | `13800000002` | `cs123` |
| 接单员 | `13800000001` | `worker123` |

这些账号只存在于本地内存种子中，禁止复制到 production。进程退出后本地数据丢失。

### 5.2 启动三个前端

打开三个新的 PowerShell 窗口，在每个窗口中设置本地 API 地址后分别运行：

```powershell
Set-Location -LiteralPath 'E:\bishe27\服务平台小程序\code'
$env:VITE_API_BASE = 'http://127.0.0.1:4176'
npm run dev:h5 --prefix apps/client
```

```powershell
Set-Location -LiteralPath 'E:\bishe27\服务平台小程序\code'
$env:VITE_API_BASE = 'http://127.0.0.1:4176'
npm run dev:h5 --prefix apps/workbench
```

```powershell
Set-Location -LiteralPath 'E:\bishe27\服务平台小程序\code'
$env:VITE_API_BASE = 'http://127.0.0.1:4176'
npm run dev:h5 --prefix apps/admin
```

Vite 会在端口占用时自动选择其他端口，以终端实际输出 URL 为准。

## 6. 发布前代码验证

每次部署 staging 或 production 前都执行：

```powershell
Set-Location -LiteralPath 'E:\bishe27\服务平台小程序\code'
npm run verify:all
```

该命令依次检查：

1. development 发布门禁；
2. 品牌矩阵；
3. ESLint；
4. 领域代码同步；
5. 自动化测试；
6. 客户 H5、客户小程序、工作台、管理台构建；
7. API 冒烟；
8. UI 冒烟。

结果写入 `code/TestEvidence/verify-summary.json`。只有 `allPassed` 为 `true` 才能继续。

还可单独执行：

```powershell
npm run check:js
npm run typecheck
npm run verify:domain
npm test
npm run build:all
npm run e2e:api
npm run e2e:ui
npm run verify:brand-matrix
```

本项目的 `typecheck` 当前实际复用 ESLint 静态检查，并不是完整的 TypeScript 编译检查；验收记录中应如实标注。

## 7. 构建全部前端

### 7.1 通用构建

```powershell
npm run build:client:h5
npm run build:client:mp
npm run build:workbench
npm run build:admin
```

或一次构建：

```powershell
npm run build:all
```

输出目录：

| 构建 | 输出目录 |
|---|---|
| 客户 H5 | `apps/client/dist/build/h5` |
| 客户小程序 | `apps/client/dist/build/mp-weixin` |
| 工作台 H5 | `apps/workbench/dist/build/h5` |
| 管理台 H5 | `apps/admin/dist/build/h5` |

### 7.2 按品牌构建客户应用

为每个真实品牌新建 `config/brands/<brand-code>.env`，内容只放公开变量：

```dotenv
VITE_BRAND_CODE=brand-a
VITE_BRAND_DISPLAY_NAME=品牌A
VITE_WECHAT_APP_ID=wx实际小程序AppID
VITE_PUBLIC_CHANNEL_ID=brand-a-mini-program
```

分别构建 H5 和小程序：

```powershell
npm run build:brand -- --brand brand-a --platform h5
npm run build:brand -- --brand brand-a --platform mp-weixin
```

独立产物保存到：

```text
BuildArtifacts/brands/brand-a/h5
BuildArtifacts/brands/brand-a/mp-weixin
```

小程序品牌构建会把对应 AppID 写入产物内的 `project.config.json`，并生成 `brand-build.json`。上传前仍需人工核对 AppID、品牌名、渠道和构建时间，防止把 A 品牌包上传到 B 品牌账号。

当前 `build:brand-matrix` 只批量构建演示品牌的 H5。真实品牌应逐个执行 H5 和 `mp-weixin` 构建。

## 8. 部署 uniCloud 后端

### 8.1 设置发布机的非敏感变量

以下示例以腾讯云 provider 为例，空间名必须换成实际 staging 空间：

```powershell
Set-Location -LiteralPath 'E:\bishe27\服务平台小程序\code'
$env:HX_CLI = 'F:\HBuilderX\cli.exe'
$env:HX_PROJECT = 'client'
$env:UNICLOUD_PROVIDER = 'tcb'
$env:UNICLOUD_SPACE = '实际-staging-空间名称或ID'
```

阿里云空间将 `UNICLOUD_PROVIDER` 改为 `aliyun`。

### 8.2 在云函数控制台配置密钥

在上传云函数之前或上传后立即配置第 4 章的服务端变量：

- `game-service`：会话、H5 token、公众号、小程序、支付、订阅消息变量；
- `wechat-callback`：`WECHAT_MP_KF_TOKEN`、`WECHAT_MP_APPID`、`WECHAT_MP_SECRET`、`INTERNAL_SECRET`、`H5_ORDER_BASE_URL`；
- 两个函数的 `INTERNAL_SECRET` 必须完全一致；
- staging 和 production 的所有密钥必须不同；
- 配置后重启/重新发布函数，使环境变量生效。

### 8.3 执行后端部署

```powershell
node scripts/deploy-unicloud.mjs
```

脚本会：

1. 从 `packages/domain` 重新生成 `uniCloud-tcb/cloudfunctions/game-service/lib/domain.cjs`；
2. 打开 `apps/client` HBuilderX 项目并关联指定云空间；
3. 上传 `game-service`；
4. 上传 `wechat-callback`；
5. 上传 `uniCloud-tcb/database` 下全部 `*.schema.json`。

任何一步失败都会中止。不要直接手工编辑生成的 `domain.cjs`；源代码应改在 `packages/domain`，然后重新构建。

第一次在新的发布机或云空间执行时，要观察 HBuilderX CLI 输出，确认它找到的是本仓库的 `uniCloud-tcb`。如果出现“找不到云函数/数据库目录”，先在 HBuilderX 中打开并关联 `apps/client` 项目和目标空间，再重试脚本，不要改成另一个未核对的空间。

### 8.4 数据库 schema

脚本会上传以下 26 个集合 schema：

```text
assignments, attachments, audit_logs, brand_config_versions, brands,
commission_rules, configs, customers, dicts, disputes, notifications,
order_logs, order_messages, orders, payments, products, refunds,
subscribe_quota, transfer_records, user_brand_roles, users, vip_list,
wallet_transactions, wallets, withdrawals, worker_profiles
```

schema 默认禁止客户端直接读写，业务应经过云函数。上传后在控制台抽查 `users`、`orders`、`payments`、`wallets`、`withdrawals` 权限，确认没有误开公网写权限。

### 8.5 手工创建数据库索引

`deploy-unicloud.mjs` 只上传 `*.schema.json`，不会自动创建 `brand-indexes.json` 中的索引。必须在 staging 验证无冲突后，在生产数据库控制台创建以下索引：

| 集合 | 索引名 | 字段 | 属性 |
|---|---|---|---|
| brands | `uniq_brand_code` | `code:1` | 唯一 |
| brands | `uniq_brand_appid` | `appId:1` | 唯一 |
| products | `idx_brand_status_sort` | `brandId:1,status:1,sort:1` | 普通 |
| orders | `uniq_order_no` | `orderNo:1` | 唯一 |
| orders | `uniq_order_idempotency` | `idempotencyScope:1,idempotencyKey:1` | 唯一、稀疏 |
| orders | `idx_brand_status_created` | `brandId:1,status:1,createdAt:-1` | 普通 |
| payments | `uniq_payment_no` | `paymentNo:1` | 唯一 |
| payments | `uniq_payment_idempotency` | `brandId:1,idempotencyKey:1` | 唯一 |
| payments | `uniq_provider_transaction` | `providerTransactionId:1` | 唯一、稀疏 |
| payments | `idx_order_status` | `orderId:1,status:1,createdAt:-1` | 普通 |
| assignments | `idx_order_created` | `orderId:1,createdAt:1` | 普通 |
| commission_rules | `uniq_brand_rule_version` | `brandId:1,version:1` | 唯一 |
| wallet_transactions | `idx_brand_order_type` | `brandId:1,orderId:1,type:1` | 普通 |
| user_brand_roles | `uniq_user_brand` | `userId:1,brandId:1` | 唯一 |
| brand_config_versions | `uniq_brand_version` | `brandId:1,version:1` | 唯一 |
| audit_logs | `idx_brand_action_created` | `brandId:1,action:1,createdAt:-1` | 普通 |

创建唯一索引前先查询重复值。若存在重复数据，应先在 staging 生成清理报告并由业务负责人确认；不要让建索引命令自动删除生产记录。

### 8.6 配置 URL 化入口

HBuilderX CLI 当前不能完成此步骤，需要在 uniCloud 控制台手工配置：

1. 为 `game-service` 开启 URL 化，绑定 API 域名；
2. 支付通知路径配置为 `/pay-notify`；
3. 最终地址必须与 `WECHAT_PAY_NOTIFY_URL` 完全一致；
4. 为 `wechat-callback` 开启 URL 化并设置独立路径，例如 `/wechat-callback`；
5. 两个入口均启用 HTTPS；
6. 如平台支持 IP 白名单、WAF、请求日志和告警，应一并启用。

支付回调由微信签名和 APIv3 解密校验保护。禁止为了调通而关闭验签。

## 9. 初始化生产数据

`scripts/api-server.mjs` 的 seed 只用于本地内存数据库，不能向生产导入。新生产空间至少需要初始化：

- 首个 `SUPER_ADMIN` 或 `ADMIN`；
- `brands` 品牌记录；
- `user_brand_roles` 人员与品牌角色；
- `products` 商品及状态；
- `commission_rules` 佣金规则版本；
- `configs` 系统配置；
- `dicts` 字典；
- 品牌公开配置、渠道引用和 Secret 引用。

当前代码没有生产首管理员的一键 bootstrap 命令。首管理员应由两人复核后通过受控数据库导入或一次性运维脚本创建，`passwordHash` 不能写明文，并要求首次登录立即改密。不要将本地 `admin123` 复制到生产。

注意：当前认证代码使用无盐 SHA-256 保存后台密码，且登录限流是单云函数实例内存计数。它们不满足高安全等级公网生产系统的最佳实践。正式开放生产前建议改为 bcrypt/scrypt/Argon2 等带盐慢哈希，并将登录限流迁移到 Redis/数据库等跨实例存储；完成前至少应限制后台访问来源、开启 MFA/统一身份认证或网关访问控制，并记录为上线风险。

初始数据写入后检查：

- 品牌 code、AppID 唯一；
- 商品都带正确 `brandId`；
- 管理、客服、接单、财务、仲裁角色按最小权限分配；
- 不同品牌人员不可读取其他品牌订单；
- 支付和消息配置只保存 `secretRef`，不把密钥正文写入业务集合；
- 所有初始写入均保留操作者、时间、审批单和导入文件 SHA-256。

## 10. 部署客户 H5、工作台和管理台

### 10.1 构建时变量

在 production H5 构建窗口中设置公开变量。使用 uniCloud transport 时清除本地 API 地址：

```powershell
Remove-Item Env:VITE_API_BASE -ErrorAction SilentlyContinue
$env:VITE_H5_ORDER_BASE_URL = 'https://h5.example.com'
$env:VITE_OAUTH_APPID = '实际服务号AppID'
```

然后执行通用构建或品牌构建。不要把 `VITE_API_BASE` 留成 `127.0.0.1`，否则用户浏览器会请求用户自己的本机。

### 10.2 部署客户 H5

```powershell
$env:HOSTING_CLIENT_SPACE = '实际客户H5托管空间'
node scripts/deploy-hosting.mjs client
```

脚本把 `apps/client/dist/build/h5` 上传到客户空间根目录 `/`。

### 10.3 部署后台 H5

```powershell
$env:HOSTING_BACKEND_SPACE = '实际后台托管空间'
node scripts/deploy-hosting.mjs backend
```

脚本上传：

- `apps/workbench/dist/build/h5` → `/workbench/`；
- `apps/admin/dist/build/h5` → `/admin/`。

不要交换两个空间变量。脚本在缺少目标空间时会拒绝使用隐式生产空间。

### 10.4 绑定域名并检查

在 uniCloud 静态托管控制台完成域名绑定、备案校验、证书部署和 CDN 刷新。然后运行：

```powershell
$env:HOSTING_CLIENT_URL = 'https://h5.example.com'
$env:HOSTING_BACKEND_URL = 'https://admin.example.com'
node scripts/verify-hosting.mjs
```

脚本检查：

- `https://h5.example.com/`；
- `https://admin.example.com/workbench/`；
- `https://admin.example.com/admin/`。

三个地址都必须返回 HTTP 200。HTTP 200 只说明静态入口可访问，还必须在浏览器中登录并执行真实 API 操作。

## 11. 部署微信小程序

每个品牌分别执行以下流程：

1. 使用品牌真实公开配置构建 `mp-weixin`；
2. 检查 `BuildArtifacts/brands/<brand>/mp-weixin/brand-build.json`；
3. 检查 `project.config.json` 的 AppID；
4. 用微信开发者工具导入该品牌产物目录；
5. 选择与 AppID匹配的小程序账号；
6. 在开发版中完成登录、商品、下单、订单、客服、订阅消息验证；
7. 上传代码，填写明确的版本号和发布说明；
8. 提交体验版给业务验收；
9. 提交微信审核；
10. 审核通过后再由有权限的人员发布正式版。

微信公众平台需要配置：

- request/uploadFile/downloadFile 合法域名；
- web-view 业务域名，即客户 H5 域名；
- 客服消息推送 URL、token 和消息加解密配置；
- 订阅消息模板及模板字段；
- 对应类目、隐私保护指引、用户协议和备案信息；
- 如使用文件上传，核对文件大小、格式和云存储权限。

目前 `manifest.json` 中 `urlCheck` 为 `false` 只便于开发，不代表正式小程序可以跳过微信平台的合法域名检查。

## 12. 配置公众号网页授权与微信支付

### 12.1 服务号网页授权

在微信公众平台配置：

- 网页授权域名：客户 H5 域名；
- JS 接口安全域名：客户 H5 域名；
- 前端 `VITE_OAUTH_APPID` 与云函数 `OAUTH_APPID` 使用同一服务号 AppID；Vite 只向前端构建暴露 `VITE_` 前缀变量；
- `OAUTH_SECRET` 只保存在云函数环境变量中，不得写入前端、构建产物或文档；
- 回调 URL 必须正确 URL 编码并使用 HTTPS。

项目使用 `snsapi_base` 获取 H5 微信内 JSAPI 支付所需 openid。staging 必须使用微信内置浏览器实测，普通桌面浏览器不能代替该验收。

### 12.2 微信支付商户平台

确认：

- 商户号已绑定实际支付 AppID；
- 已开通所需 JSAPI/H5 支付能力；
- APIv3 密钥、商户私钥、证书序列号匹配；
- 支付通知地址是 URL 化后的 `/pay-notify`；
- 客户 H5 域名、H5 支付域名和回跳地址已配置；
- 商户平台的运营主体、结算账户和权限均正常；
- 退款、转账到零钱等能力如需使用，已单独开通并完成风控配置。

在安全的 staging 环境验证请求签名配置：

```powershell
node scripts/verify-wechat-pay-sign.mjs
```

该脚本会使用假 openid 发起签名/请求验证。缺少变量会直接失败；网络或微信业务错误需要结合输出判断。不要把脚本输出中的敏感信息保存到公开日志。

### 12.3 staging 最小金额验收

至少完成：

- 微信内 1 分钱 JSAPI 支付；
- 微信外 H5/MWEB 支付；
- 成功回调、重复回调、乱序回调；
- 错误金额或错误商户回调不推进订单；
- 1 分钱退款及退款状态回写；
- 退款后的佣金冲正和钱包账本对账；
- 提现审批、出款失败、重试和凭证留档。

验收完成后按财务流程退款，不要保留测试资金悬挂。

## 13. 微信客服回调与订阅消息

### 13.1 客服回调

将 `wechat-callback` 的 URL 化地址填写到小程序客服消息推送配置中，并使用与 `WECHAT_MP_KF_TOKEN` 相同的 token。

验证：

1. 微信平台的 URL 校验能够通过；
2. 错误签名请求被拒绝；
3. 用户从小程序进入客服并发送消息后能收到 H5 服务链接；
4. 链接指向正确品牌和 `H5_ORDER_BASE_URL`；
5. `INTERNAL_SECRET` 缺失时系统应 fail-closed，不得绕过鉴权；
6. A 品牌用户不能收到 B 品牌链接。

### 13.2 订阅消息

在每个小程序账号建立所需模板，将模板 ID 写入 `WECHAT_MP_TEMPLATE_IDS` JSON。检查代码中的业务 key 与平台模板一致，例如 `ORDER_STATUS`。

实测用户授权、拒绝、配额耗尽、模板失效和发送失败场景。订阅消息发送成功的 API 响应不能替代用户手机实际收信检查。

## 14. 数据迁移

旧数据升级应先导出、再离线转换、再导入新空间，禁止直接在生产原数据上试跑。

第二阶段模型迁移脚本默认只输出报告：

```powershell
npm run migrate:phase2 -- --input 'D:\secure-backup\snapshot.json'
```

确认报告后写入一个新文件：

```powershell
npm run migrate:phase2 -- --input 'D:\secure-backup\snapshot.json' --write --output 'D:\secure-backup\snapshot-phase2.json'
```

脚本禁止覆盖原备份。迁移后应重点核对：

- 订单品牌快照和佣金规则快照；
- `paymentId`、支付单和退款单关联；
- 提现状态迁移；
- 异议状态、证据和时间线；
- 钱包流水方向、账户类型和冲正类型；
- orphan 报告必须为空或逐条人工处理；
- 迁移前后集合数量、资金总额和订单总额一致。

先导入 staging/隔离空间完成业务回归和财务对账，审批后才允许安排生产迁移窗口。

## 15. 备份、恢复演练与回滚

### 15.1 真实生产备份

生产备份必须使用 uniCloud/云数据库控制台或经过认证的数据库导出能力，覆盖全部 26 个集合，并记录：

- 备份 ID；
- 开始/完成时间；
- 云空间 ID；
- 每个集合的记录数；
- 导出文件 SHA-256；
- 操作者和复核人；
- 加密存储位置及保留期限；
- 恢复演练结果。

`npm run backup:local` 只导出本地内存镜像数据，`restore:drill` 也只恢复到隔离的内存数据库。它们用于验证备份格式和流程，不能被描述成真实生产备份或生产恢复。

本地演练示例：

```powershell
npm run backup:local -- --dir '.\backups' --retention-days 30
npm run restore:drill -- --dir '.\backups'
```

### 15.2 生成回滚清单

当备份目录中存在日期目录和 `manifest.json` 时：

```powershell
npm run rollback:bundle -- --backup-dir 'D:\secure-backup\platform' --output 'D:\release\rollback-production.json'
```

生成的 JSON 只引用备份和品牌构建产物，不会覆盖生产数据。检查 SHA-256、备份 ID、构建品牌/AppID 后，把路径写入 `ROLLBACK_BUNDLE_PATH`。

### 15.3 回滚顺序

发生严重故障时：

1. 暂停下单、支付、退款、提现、派单等写入口；
2. 保留现场日志、请求 ID、微信交易号和当前数据库快照；
3. 将客户 H5、工作台、管理台切回上一已验证版本；
4. 将两个云函数切回上一已验证版本；
5. 校验备份 manifest 与 SHA-256；
6. 在隔离空间完成恢复和资金对账；
7. 由业务、技术、财务负责人共同审批是否恢复生产数据；
8. 恢复后重新执行登录、品牌隔离、订单、支付回调、钱包和托管健康检查；
9. 分批恢复写入口并持续监控。

禁止未演练就直接把备份导回生产，也禁止只回滚前端而忽略云函数/数据库模型兼容性。

## 16. staging 发布流程

建议严格按以下顺序：

1. 锁定待发布代码和品牌配置；
2. 安装锁定依赖；
3. 执行 `npm run verify:all`；
4. 注入 staging 的模式、空间、域名和 Secret；
5. 执行 staging 门禁；
6. 备份 staging 数据；
7. 上传云函数和 schema；
8. 手工创建/核对数据库索引；
9. 配置 URL 化、微信客服回调和支付回调；
10. 初始化或迁移 staging 数据；
11. 构建并部署客户 H5、工作台、管理台；
12. 逐品牌构建和上传小程序体验版；
13. 执行真实微信、真实支付和品牌隔离验收；
14. 保存截图、原始 JSON、云日志、交易号、对账结果和签字记录。

门禁命令：

```powershell
$env:APP_ENV = 'staging'
$env:API_MODE = 'unicloud'
$env:PAYMENT_MODE = 'wechat'
$env:MESSAGE_MODE = 'wechat'
$env:BRAND_CODE = '实际品牌code'
npm run release:preflight -- --environment staging
```

`release:preflight` 只验证必要变量存在、组合正确、URL 使用 HTTPS，并不会验证变量值真的属于目标商户或目标空间，所以仍需人工复核。

## 17. production 正式发布流程

### 17.1 发布前一天

- staging 全项验收通过；
- 微信小程序审核通过，正式版本待发布；
- 确认发布窗口和回滚负责人；
- 确认生产数据库备份能力可用；
- 核对域名证书有效期、空间配额、云函数超时/内存配置；
- 核对商户余额、退款权限、转账权限和告警接收人；
- 冻结本次发布范围，不混入未验收需求。

### 17.2 发布当日

1. 从真实生产数据库导出全量备份；
2. 校验集合数量、文件 SHA-256，并在隔离空间执行恢复演练；
3. 生成并复核回滚清单；
4. 设置 production 门禁变量；
5. 再次执行 `verify:all` 和 production preflight；
6. 先部署后端和 schema，确认云函数日志无启动错误；
7. 创建或核对索引；
8. 部署客户 H5 和后台 H5；
9. 运行静态托管健康检查；
10. 执行管理员登录、品牌隔离和只读查询；
11. 执行一笔最小金额支付、回调、退款和对账；
12. 发布各品牌小程序正式版；
13. 观察 30～60 分钟核心指标后结束发布窗口。

production 门禁：

```powershell
$env:APP_ENV = 'production'
$env:API_MODE = 'unicloud'
$env:PAYMENT_MODE = 'wechat'
$env:MESSAGE_MODE = 'wechat'
$env:BRAND_CODE = '实际品牌code'
$env:RELEASE_APPROVED = 'true'
$env:DB_BACKUP_ID = '真实生产备份ID'
$env:ROLLBACK_BUNDLE_PATH = 'D:\release\rollback-production.json'
npm run release:preflight -- --environment production
```

Secret 应由 CI/CD Secret 管理或受控发布终端注入，不建议在 PowerShell 历史中逐条粘贴。

## 18. 发布后验收清单

### 18.1 前端

- 客户域 `/`、后台域 `/workbench/`、`/admin/` 均为 200；
- 页面没有请求 `127.0.0.1`、示例域名或错误品牌域名；
- 客户 H5、工作台、管理台均能真实调用 `game-service`；
- 刷新 hash 路由后页面仍能打开；
- A/B 品牌名称、主题、AppID、商品、客服联系信息正确；
- 浏览器控制台无持续错误，移动端无白屏。

### 18.2 后端与数据库

- `game-service` 和 `wechat-callback` 部署版本正确；
- 26 个 schema 存在，16 个索引正确；
- Secret 无缺失且没有打印到日志；
- 管理员、客服、接单员、财务和仲裁权限正确；
- A 品牌账号读取 B 品牌数据返回 403/404；
- 审计日志包含关键写操作的 before/after；
- 定时超时任务仅在明确配置的调度器中运行，避免重复调度。

### 18.3 交易与资金

- 下单幂等：重复提交不创建重复订单；
- 支付幂等：重复回调不重复入账；
- 抢单并发：两个接单员只有一个成功；
- 退款后订单、支付、退款、佣金和钱包流水一致；
- 提现状态机、审批、出款凭证和失败重试正确；
- 钱包重算结果与账本一致；
- 微信商户平台交易金额与数据库对账一致。

### 18.4 监控

至少监控：

- 云函数错误率、超时率、调用量；
- 支付回调失败、验签失败、金额不一致；
- 退款/转账失败；
- 登录失败和账号锁定；
- 订单超时任务执行结果；
- 数据库容量、慢查询和索引命中；
- 静态托管 4xx/5xx 与证书到期时间；
- 告警 webhook 的真实送达。

当前 `scripts/alert.mjs` 是“控制台 + 文件 + 可选 webhook”的基础通道，正式告警链路需要单独做接收端、重试、升级和静默策略验证。

## 19. 常见故障处理

### 19.1 `缺少 UNICLOUD_SPACE`

原因：部署脚本采用 fail-closed，不允许猜测生产空间。

处理：运行 `node scripts/list-unicloud.mjs`，人工核对后设置准确的 `UNICLOUD_SPACE`。

### 19.2 provider 不识别或找不到空间

处理：将 `UNICLOUD_PROVIDER` 明确设置为 `tcb` 或 `aliyun`；确认 HBuilderX 登录账号和项目关联空间正确。

### 19.3 找不到云函数或 schema

处理：确认当前目录是 `code`，HBuilderX 项目为 `apps/client`，仓库根存在 `uniCloud-tcb`；先在 HBuilderX 图形界面关联空间，再重试脚本。

### 19.4 H5 页面 200，但操作提示后端不可用

原因通常是 H5 构建残留本地 `VITE_API_BASE`，或正式页面没有可用的 `uniCloud.callFunction`。

处理：检查浏览器 Network/Console 和构建环境；清除 `VITE_API_BASE` 后重建，并确认 H5 与目标云空间关联。不能只检查静态首页 200。

### 19.5 `/workbench/` 或 `/admin/` 404

处理：确认使用 `deploy-hosting.mjs backend`，上传前缀正确，域名绑定到后台空间；不要把两个 H5 产物上传到根目录。

### 19.6 微信支付提示签名、证书或 AppID 错误

逐项核对商户号、证书序列号、私钥、APIv3 密钥和已绑定 AppID；确认私钥换行被正确还原；在 staging 运行签名验证脚本。不要通过关闭验签解决。

### 19.7 支付成功但订单未更新

检查微信商户平台通知记录、`WECHAT_PAY_NOTIFY_URL`、URL 化路径、云函数日志、验签/解密错误和金额校验；使用订单号与微信交易号关联排查，避免人工重复入账。

### 19.8 客服回调校验失败

核对回调 URL、`WECHAT_MP_KF_TOKEN`、签名参数和微信平台消息模式；确认 callback 云函数已配置小程序 AppID/Secret 和 `INTERNAL_SECRET`。

### 19.9 唯一索引创建失败

说明已有重复品牌、订单号、支付号、交易号或幂等键。先导出重复记录报告，在 staging 确定合并/保留规则并审批；禁止直接删除生产重复项。

### 19.10 Windows 构建打印完成后返回 `0xC0000409`

品牌构建脚本会在确认本次产物存在、时间正确且包含品牌 code 后将该 DCloud 退出异常按成功收口。仍需检查 `brand-build.json` 和实际产物；如果产物缺失或品牌不匹配，按失败处理。

## 20. 安全与上线红线

- 所有外网地址必须使用 HTTPS；
- Secret 不得进入前端、品牌 env、Git 或公开日志；
- 生产和 staging 空间、数据、密钥必须隔离；
- 数据库集合禁止客户端绕过云函数直接写入；
- 所有订单、支付、退款、提现写操作必须保留幂等键和审计日志；
- 支付回调必须验签、解密并校验商户号、订单号和金额；
- 生产恢复必须先在隔离空间演练并审批；
- 禁止使用本地 seed 作为生产初始数据；
- 禁止把本地 Mock 结果描述为真实支付或真实云环境验证；
- 未解决无盐 SHA-256 密码和单实例内存限流风险前，不应把后台直接暴露为无额外保护的公网管理系统；
- 多品牌使用不同微信/支付账号时，必须证明 Secret 按品牌隔离，不能只证明前端皮肤和 AppID不同；
- 真实生产发布前必须有可用备份、回滚版本、负责人和明确停止条件。

## 21. 本项目当前可复用的发布证据

截至 2026-08-31，本地完整验证结果位于：

```text
code/TestEvidence/verify-summary.json
```

其中 8 项本地验证均通过。现有回滚示例位于：

```text
code/TestEvidence/phase3-rollback-bundle.json
```

现有品牌演示产物位于：

```text
code/BuildArtifacts/brands/demo-a
code/BuildArtifacts/brands/demo-b
```

这些文件可以作为发布流程模板，但其中的 demo AppID、演示品牌、内存备份和本地验证证据不能直接当作生产证据。正式上线需要重新生成真实品牌产物、真实数据库备份、真实 staging 验收记录和 production 回滚清单。

## 22. 建议保存的发布归档

每次 staging/production 发布建议按版本建立只读归档目录，至少包含：

```text
release-<version>/
  verify-summary.json
  release-preflight.json
  brand-build-*.json
  database-manifest.json
  database-sha256.txt
  rollback-bundle.json
  hosting-health.txt
  cloudfunction-version.txt
  database-indexes.txt
  staging-evidence/
  production-checklist.md
  approval-record.md
```

归档中不得保存明文 Secret。涉及手机号、openid、订单或资金明细的证据应脱敏并加密保存。
